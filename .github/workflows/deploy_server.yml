on:
  push:

jobs:
  server_only_with_local:
    uses: ./.github/workflows/cluster_deploy.yml
  with_online_trainer:
    uses: ./.github/workflows/cluster_deploy.yml
    with:
      online_trainer: true